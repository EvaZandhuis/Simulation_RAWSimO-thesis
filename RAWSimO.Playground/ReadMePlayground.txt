
for running a single file:
playground, execute instance (1)

creating multiple seeds:
playground, execute directory (8)
(this one works with the instance, not the layout file. The results are the same, layout just uses more space)

Manual copy results folders naar een zelfde directory

DataPrep, process results (1)
Gaat naar Dataprocessor, PrepareAllResults van alles in die directory (=> dus verschillende directories per scenario maken)
te weinig datapoints om mee te plotten met replen uit en 30min simtime, maar footprints kan wel.

met dataprep scatterplot (4) gebruik je footprints file, dan krijg je scatterplot van alle seeds per scenario
klik op gnuplot app, dat maakt pdf file met results